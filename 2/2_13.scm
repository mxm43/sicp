; percent = [(a + a * p) * (b + b * q) - (a - a * p) * (b - b * q)] /
;           [(a + a * p) * (b + b * q) + (a - a * p) * (b - b * q)]
;         = (p + q) / (1 + p * q)
;
; if p and q are small enough, p * q approximately equals to 0, percent
; approximately equals to (p + q).
